{
  "name": "@vunachain/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "source venv/bin/activate && python3 manage.py runserver",
    "test": "source venv/bin/activate && python3 manage.py test",
    "install": "python3 -m venv venv && source venv/bin/activate && pip3 install -r requirements.txt"
  }
}
